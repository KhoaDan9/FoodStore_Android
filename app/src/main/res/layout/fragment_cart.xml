<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_margin="10dp">
    <Button
        android:id="@+id/btn_back_to_menu_staff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Trở về trang menu"
        android:layout_marginBottom="15dp"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcv_cart"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_back_to_menu_staff"
        />
    <TextView
        android:id="@+id/tv_cart_total_bill1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Tổng thành tiền: "
        android:textSize="18dp"
        android:layout_below="@+id/rcv_cart"/>
    <TextView
        android:id="@+id/tv_cart_total_bill2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:layout_marginTop="10dp"
        android:textSize="18dp"
        android:layout_toRightOf="@+id/tv_cart_total_bill1"
        android:layout_below="@+id/rcv_cart"
        />
</RelativeLayout>